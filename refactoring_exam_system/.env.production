# Production build — used automatically by `npm run build`
# Public URLs only (no secrets)

VITE_API_BASE_URL=https://maramhasri.pythonanywhere.com
VITE_WS_BASE_URL=wss://abc-maramHasri.pythonanywhere.com
# Optional: set when frontend is hosted (otherwise uses window.location.origin)
# VITE_FRONTEND_BASE_URL=https://your-frontend.example.com
